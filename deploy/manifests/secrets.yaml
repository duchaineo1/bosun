# Edit values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: bosun-secrets
  namespace: bosun
type: Opaque
stringData:
  db-user: awxng
  db-password: changeme
  jwt-secret: changeme-use-a-real-secret
# ghcr-pull-secret is created separately via:
#   make create-pull-secret GITHUB_USER=<user> GITHUB_PAT=<token>
