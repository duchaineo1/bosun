apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: bosun
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
    spec:
      containers:
      - name: ui
        image: ghcr.io/duchaineo1/ui:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: bosun
spec:
  selector:
    app: ui
  ports:
  - port: 80
